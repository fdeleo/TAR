#include <stdio.h>
#include <string.h>


int main()
{
   char *haystack = "userid:nick.name:email@poli.ufrj.br:seila";
   const char needle[9] = "nick.name";
   char *ret;

   ret = strstr(haystack, needle);

   printf("The substring is: %s\n", ret);

   return(0);
}
